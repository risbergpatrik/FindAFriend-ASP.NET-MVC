@model IEnumerable<FindAFriend.Models.Friends>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FriendWith)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FriendWith)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FriendshipID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FriendshipID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FriendshipID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
